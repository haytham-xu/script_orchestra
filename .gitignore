
# Global
/.claude
/.playwright-mcp
venv
__pycache__
## All SQLite databases (runtime data, never committed)
**/*.db
## All per-tool runtime settings (convention: every tool uses settings.json)
backend/**/settings.json


# Tool specified
## Photo Classifier runtime storage
backend/photo_classifier/.thumbnail_cache

# Test runtime data — generated during test execution, never committed.
# Convention: always placed under test_runtime/ inside the test directory.
**/test_runtime/

# Assistant runtime storage
backend/assistant/media/
